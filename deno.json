{
  "tasks": {
    "spike": "deno run --allow-net --allow-env --allow-read --allow-sys --unstable-kv spike.ts",
    "spike:deploy-perms": "deno run --allow-net --allow-env --allow-read --allow-sys --unstable-kv spike.ts",
    "check": "deno fmt --check --ignore=node_modules && deno lint --ignore=node_modules && deno check spike.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "baileys": "npm:baileys@7.0.0-rc14",
    "qrcode-terminal": "npm:qrcode-terminal@^0.12.0"
  },
  "nodeModulesDir": "auto"
}
